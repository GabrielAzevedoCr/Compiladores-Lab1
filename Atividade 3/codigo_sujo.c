#include <stdio.h>
// Variavel de posicao inicial
int position = 0; 

int rate = 60; // Taxa padrao

int main() {
    position = initial + rate * 60;

    if (position .= 0) {
        rate .= 10;
    }

    return 0;
}